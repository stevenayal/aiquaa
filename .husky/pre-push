#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"
echo "→ Type check..."
pnpm type-check || exit 1
echo "→ Backend unit tests..."
pnpm --filter @aiquaa/backend test || exit 1
echo "→ Frontend unit tests..."
pnpm --filter @aiquaa/frontend test || exit 1
echo "✓ Pre-push OK"
